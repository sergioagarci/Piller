<%= form_for(current_usuario.relationships.find_by(followed_id: @usuario), 
												   html: { method: :delete }, remote: true) do |f| %>
  <%= f.submit "Dejar de seguir", class: "btn btn-large" %>
<% end %>