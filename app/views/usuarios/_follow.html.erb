<%= form_for(current_usuario.relationships.build(followed_id: @usuario.id), remote: true) do |f| %>
  <div><%= f.hidden_field :followed_id %></div>
  <%= f.submit "Seguir", class: "btn btn-large btn-primary" %>
<% end %>